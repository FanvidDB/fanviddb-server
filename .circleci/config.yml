version: 2.1

orbs:
  python: circleci/python@1.3.2

jobs:
  lint:
    executor:
      name: python/default
      tag: 3.7.10
    parameters:
      lint-tool:
        type: string
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: pip
      - run: << parameters.lint-tool >>

workflows:
  main:
    jobs:
      - python/test:
          test-tool: pytest
          version: 3.7.10
      - lint:
          name: mypy
          lint-tool: mypy .
      - lint:
          name: black
          lint-tool: black . --check
      - lint:
          name: isort
          lint-tool: isort . --check
      - lint:
          name: flake8
          lint-tool: flake8
      - lint:
          name: vulture
          lint-tool: vulture .
